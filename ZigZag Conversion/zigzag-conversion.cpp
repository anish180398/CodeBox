#include<iostream>
#include<string>

using namespace std;

class Solution {
public:
    string convert(string s, int rows) {
        int length = s.length();
		int zigZagSkips = rows - 2;
		int columns = (length / )
		vector<vector<char>> zigzag = (rows, vector<char>())
    }
};